{
  "devDependencies": {
    "turbo": "^2.5.8"
  },
  "name": "diagnostics",
  "packageManager": "pnpm@10.17.1",
  "pnpm": {
    "overrides": {
      "cookie": ">=0.7.0",
      "esbuild": "^0.25.10",
      "scheduler": ">=0.26.0",
      "vitest>jsdom": "26.1.0"
    },
    "onlyBuiltDependencies": [
      "@heroui/shared-utils",
      "@parcel/watcher",
      "@swc/core",
      "@tailwindcss/oxide",
      "esbuild",
      "lmdb",
      "msgpackr-extract",
      "sharp",
      "vue-demi"
    ]
  },
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "format": "turbo run format",
    "lint": "turbo run lint",
    "test": "turbo run test --cache=local:r,remote:r"
  },
  "type": "module",
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ]
}
