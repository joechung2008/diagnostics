{
  "$schema": "https://turborepo.com/schema.v2.json",
  "globalPassThroughEnv": ["CHROME_BIN"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [
        ".next/**",
        ".output/**",
        ".svelte-kit/**",
        "build/**",
        "dist/**"
      ]
    },
    "format": {},
    "lint": {},
    "test": {
      "dependsOn": ["^build"]
    }
  }
}
